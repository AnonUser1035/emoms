{
  "name": "emoms-checkin-worker",
  "private": true,
  "version": "0.0.0",
  "description": "Stateless Cloudflare Worker backing the emoms shared community heatmap: a fixed-timezone daily check-in counter over D1.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260706.1",
    "typescript": "~5.9.0",
    "wrangler": "^4.0.0"
  }
}

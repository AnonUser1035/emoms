# Copy to `.env` for local dev. Optional — the app works fully without it,
# just without the shared community heatmap (checkin/heatmap silently no-op).
#
# URL of the deployed checkin Worker (see /worker/README.md).
VITE_CHECKIN_WORKER_URL=https://emoms-checkin.ryanbohluli.workers.dev
